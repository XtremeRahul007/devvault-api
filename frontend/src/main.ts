import "./styles/main.css";

/*
async function initApp() {
    try {
        const files = await getFiles();

        console.log(files);
    } catch (error) {
        console.error(error);
    }
}

initApp();*/